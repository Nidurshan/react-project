import React from "react";

const Header = () => {

    const headerStyle = {background:"mediumBlue", color:"red"};

    return (
        <header style={headerStyle}>
            <h1>To do list</h1>
        </header>
    )
}

export default Header;